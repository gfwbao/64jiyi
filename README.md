# 填空

一个中文填空小游戏。

纯静态网页,无依赖,无后端。直接用浏览器打开 `index.html` 即可运行,或部署到任意静态托管服务。

## 部署

本仓库通过 GitHub Pages 托管,绑定自定义域名(见 CNAME)。

## 本地运行

直接双击 `index.html`,或:

```
python3 -m http.server 8080
```

然后访问 http://localhost:8080
